Place local board images here for the manual preview and future GUI work.

Expected default filenames:

- `germany.png`
- `usa.png`

PNG is recommended because the Tkinter preview tool uses `PhotoImage`.
